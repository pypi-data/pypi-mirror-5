#!/usr/bin/env python
# coding=utf-8
import sys

try:
    import nodist.__main__
except ImportError:
    sys.stderr.write("nodist: Cannot import the Python package 'nodist'.\n")
    sys.exit(-1)
if __name__ == "__main__":
    nodist.__main__.main(sys.argv[1:])
