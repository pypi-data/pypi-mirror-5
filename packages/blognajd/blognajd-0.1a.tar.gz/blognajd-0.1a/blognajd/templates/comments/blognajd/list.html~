{% load comments_xtd %}
{% for comment in comment_list %}
<div class="row-fluid">
  <div class="cmt offset{{ comment.level }} span{{ 12|add:-comment.level }}">
    <div class="headline">
      {{ comment.submit_date|date:"F j, Y" }}&nbsp;-&nbsp;
      {% if comment.user_url %}
      <a href="{{ comment.user_url }}">{{ comment.name }}</a>
      {% else %}{{ comment.name }}{% endif %}
      {% if comment.allow_thread %}&nbsp;-&nbsp;
      <a href="{{ comment.get_reply_url }}">{% trans "Reply" %}</a>{% endif %}
    </div>
    <div class="text">{{ comment.comment|render_markup_comment }}</div>
  </div>
</div>
{% endfor %}
