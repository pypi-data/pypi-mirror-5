{% load comments_xtd %}
{% for comment in comment_list %}
<div class="comment row-fluid">
  <div class="offset{{ comment.level }} span{{ 12|add:comment.level }}">
    <div class="row-fluid">
      <div class="span10">
	<span class="date">{{ comment.submit_date|date:"F j, Y" }}</span>
	&nbsp;-&nbsp;
	{% if comment.user_url %}
	<a href="{{ comment.user_url }}">{{ comment.name }}</a>
	{% else %}{{ comment.name }}{% endif %}
      </div>
      <div class="span2 pull-right text-right">
	{% if comment.allow_thread %}
	<small><a href="{{ comment.get_reply_url }}">reply</a></small>
	{% endif %}
      </div>
    </div>
    <div class="row-fluid">
      <div class="span12 text">
	{{ comment.comment|render_markup_comment }}
      </div>
    </div>
  </div>
</div>
{% endfor %}
