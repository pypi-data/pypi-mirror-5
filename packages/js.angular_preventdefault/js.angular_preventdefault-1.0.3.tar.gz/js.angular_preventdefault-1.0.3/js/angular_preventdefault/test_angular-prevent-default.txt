How to use?
===========


You can import ``angular_prevent_default`` from ``js.angular_preventdefault`` and ``need`` it where you want
these resources to be included on a page::

  >>> from js.angular_preventdefault import angular_prevent_default 
  >>> angular_prevent_default.need()

