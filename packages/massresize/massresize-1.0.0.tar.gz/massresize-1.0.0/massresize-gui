#!/usr/bin/python

import Tkinter, tkFileDialog
from libmassresize import massresize

root = Tkinter.Tk()
dirname = tkFileDialog.askdirectory(parent=root, title='Please select a directory')
massresize(dirname, 1024)