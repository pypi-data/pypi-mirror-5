#!/usr/bin/env mio

Foo = Object clone do (
    foo = method(
        "foo" println
    )
)

Bar = Foo clone do (
    foo = method(
        super foo
        "bar" println
    )
)

World = Bar clone do (
    foo = method(
        super foo
        "Hello World!" println
    )
)
