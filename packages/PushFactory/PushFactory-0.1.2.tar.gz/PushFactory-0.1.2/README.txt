===========
PushFactory
===========

PushFactory provides a generic interface to multiple backends for sending push messages to mobile devices. It currently supports APN (Apple Push Notifications) and GCM (Google Cloud Messaging).
