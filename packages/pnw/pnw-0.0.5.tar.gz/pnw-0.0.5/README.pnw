#@import(pnw.pnw)

#@chunk(README,format=rst)
PNW
===

#@include(moduledoc)

Nutshell
--------

Here is a brief example of a pnw file::

    
    $@path(../..)
    $@path(..)
    $@import(latex-blocks.tex)
    $@import(md-blocks)
    $@import(pnw.pnw)
    $@include(ltxheader)
    $@chunk(test,format=md)
    
       A block of text
    $@end

    $@chunk(pnw,format=rst)
    PNW
    ===

    $@include(moduledoc)
    $@include(test)
    $@end
#@end    
