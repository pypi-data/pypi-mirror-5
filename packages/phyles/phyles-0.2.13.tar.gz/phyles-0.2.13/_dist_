#! /bin/csh -f

set PACKAGE="phyles"
set PVER="0.1.1"

rm -rf ${HOME}/Code/python/${PACKAGE}

cd ${HOME}/Code/${PACKAGE}

cd docsrc/
make clean
make html
cd ..
/usr/local/bin/python setup.py sdist
sudo /usr/local/bin/easy_install dist/${PACKAGE}-${PVER}.tar.gz

