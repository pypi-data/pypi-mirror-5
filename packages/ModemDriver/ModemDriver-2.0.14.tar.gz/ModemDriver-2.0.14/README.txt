This library is used to control a GSM or CDMA modem with AT command. It can send / receive SMS, USSD, and using existing applications on the SIM card. It depends on:

- pySerial (http://pyserial.sourceforge.net)
- python-messaging (https://github.com/pmarti/python-messaging)

It also can used as command line.
