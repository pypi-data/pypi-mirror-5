file:
  use_sendfile = 1
  iterable = fileobj
  iterator = NULL <---
  current_chunk = headers
  current_chunk_p = 0

  nach header fertig:
    current_chunk = NULL
    current_chunk_p = fileobj.fileno()
