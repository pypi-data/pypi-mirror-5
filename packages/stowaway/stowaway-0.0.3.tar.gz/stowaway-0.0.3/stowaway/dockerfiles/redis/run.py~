#!/bin/python
# -*- coding: utf-8 -*-
import os
import subprocess

PATH = '/etc/redis/redis.conf'
config = open(PATH, 'r').read()
config = config.replace('bind 127.0.0.1', '')
open(PATH, 'w').write(config)

subprocess.Popen(["/usr/bin/redis-server", "/etc/redis/redis.conf"])
