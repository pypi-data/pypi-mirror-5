import sys

from rdflib.plugin import _plugins

KIND = sys.argv[1]

p = {}

for (name, kind), plugin in _plugins.items(): 
    if kind == KIND: 
        p[name]="%s.%s"%(plugin.module_path, plugin.class_name)

l1=max(len(x) for x in p)
l2=max(len(x.__module)+len
for c 
